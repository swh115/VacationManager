@model VacationManager.Models.Vacation
@{
    Layout = null;
}

<!DOCTYPE html>
<html>

<head>
    <title>Add Vacation</title>

    <!-- ✅ jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- ✅ jQuery UI -->
    <link rel="stylesheet" href="https://code.jquery.com/ui/1.13.2/themes/base/jquery-ui.css" />
    <script src="https://code.jquery.com/ui/1.13.2/jquery-ui.min.js"></script>

    <!-- ✅ jQuery UI Timepicker Addon -->
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/jquery-ui-timepicker-addon/1.6.3/jquery-ui-timepicker-addon.min.css" />
    <script
        src="https://cdnjs.cloudflare.com/ajax/libs/jquery-ui-timepicker-addon/1.6.3/jquery-ui-timepicker-addon.min.js"></script>

    <!-- ✅ jQuery Validation (from Cloudflare, not jsDelivr) -->
    <script src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.5/dist/jquery.validate.min.js"></script>


</head>


<body style="font-family: sans-serif; display: flex; justify-content: center; align-items: center; height: 100vh;">
    <div
        style="background-color: white; padding: 2rem; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.1); width: 400px;">
        <h2 style="text-align:center;">Add Vacation</h2>
        <form asp-action="Add" asp-controller="VacationForm" method="post">
            <label for="Description">Description</label>
            <input name="Description" id="description" required
                style="width:100%; padding: 0.5rem; margin-bottom: 1rem;" />

            <label for="FromDate">From Date</label>
            <input name="FromDate" id="from" required style="width:100%; padding: 0.5rem; margin-bottom: 1rem;" />

            <label for="ToDate">To Date</label>
            <input name="ToDate" id="to" required data-rule-greaterThanFrom="true"
                style="width:100%; padding: 0.5rem; margin-bottom: 1rem;" />


            <label for="DurationDays">Duration (days)</label>
            <input name="DurationDays" id="DurationDays" readonly
                style="width:100%; padding: 0.5rem; margin-bottom: 1rem;" />

            <label>
                <input type="checkbox" id="enableTime" />
                Select specific time
            </label>



            <button type="submit"
                style="width:100%; padding: 0.75rem; background:#007BFF; color:white; border:none; border-radius:5px;">
                Save
            </button>
        </form>

    </div>

    <script src="https://cdn.jsdelivr.net/jquery.validation/1.19.5/jquery.validate.min.js"></script>

    <script>
        $(function () {
            const dateOnly = {
                dateFormat: 'yy-mm-dd',
                minDate: 0
            };

            const dateTime = {
                dateFormat: 'yy-mm-dd',
                timeFormat: 'HH:mm',
                controlType: 'select',
                oneLine: true,
                minDate: 0
            };

            function calculateDuration() {
                const from = $("#from").datetimepicker("getDate");
                const to = $("#to").datetimepicker("getDate");

                if (from && to && to >= from) {
                    const diff = Math.floor((to - from) / (1000 * 60 * 60 * 24));
                    $("#DurationDays").val(diff);
                } else {
                    $("#DurationDays").val('');
                }
            }

            function toggleTimePicker() {
                $("#from, #to").datepicker("destroy").datetimepicker("destroy");

                if ($("#enableTime").is(":checked")) {
                    $("#from, #to").datetimepicker(dateTime);
                } else {
                    $("#from, #to").datepicker(dateOnly);
                }

                // rebind minDate logic
                const fromDate = $("#from").datetimepicker("getDate");
                if (fromDate) {
                    $("#to").datetimepicker("option", "minDate", fromDate);
                }

                calculateDuration();
            }

            $.validator.addMethod("greaterThanFrom", function (value, element) {
                const from = $("#from").datetimepicker("getDate");
                const to = $("#to").datetimepicker("getDate");
                return !from || !to || to >= from;
            }, "To Date must be after or equal to From Date.");

            $("#to").attr("data-rule-greaterThanFrom", "true");

            $("#enableTime").on("change", toggleTimePicker);
            $("#from, #to").on("change", calculateDuration);
            $("form").validate();

            // Initial setup
            toggleTimePicker();
        });
    </script>



</body>

</html>
